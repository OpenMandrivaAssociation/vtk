diff -up VTK-9.1.0/Rendering/ContextOpenGL2/vtkOpenGLContextDevice2D.cxx.omv~ VTK-9.1.0/Rendering/ContextOpenGL2/vtkOpenGLContextDevice2D.cxx
--- VTK-9.1.0/Rendering/ContextOpenGL2/vtkOpenGLContextDevice2D.cxx.omv~	2022-03-29 01:16:27.905328463 +0200
+++ VTK-9.1.0/Rendering/ContextOpenGL2/vtkOpenGLContextDevice2D.cxx	2022-03-29 01:17:34.204727416 +0200
@@ -1122,21 +1122,27 @@ void vtkOpenGLContextDevice2D::DrawPoint
       cbo->Program->SetUniformi("texture1", tunit);
     }
 
+#ifdef GL_POINT_SPRITE
     // We can actually use point sprites here
     if (this->RenderWindow->IsPointSpriteBugPresent())
     {
       glEnable(GL_POINT_SPRITE);
       glTexEnvi(GL_POINT_SPRITE, GL_COORD_REPLACE, GL_TRUE);
     }
+#endif
+#ifdef GL_POINT_SPRITE_COORD_ORIGIN
     glPointParameteri(GL_POINT_SPRITE_COORD_ORIGIN, GL_LOWER_LEFT);
+#endif
 
     glDrawArrays(GL_POINTS, 0, n);
 
+#ifdef GL_POINT_SPRITE
     if (this->RenderWindow->IsPointSpriteBugPresent())
     {
       glTexEnvi(GL_POINT_SPRITE, GL_COORD_REPLACE, GL_FALSE);
       glDisable(GL_POINT_SPRITE);
     }
+#endif
 
     if (sprite)
     {
